# Adres API Spring Boot. Lokalnie: cd ../backend && docker compose up -d && ./gradlew bootRun
# Backend dopuszcza w CORS http://localhost:3000 (CORS_ALLOWED_ORIGINS).
NEXT_PUBLIC_API_URL=http://localhost:8080
